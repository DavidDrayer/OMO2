<?
	// Charge les librairies
	require_once($_SERVER['DOCUMENT_ROOT']."/config.php");
	require_once($_SERVER['DOCUMENT_ROOT']."/shared_functions.php");
	require_once($_SERVER['DOCUMENT_ROOT']."/shared/telegram.php");
	
	// Initialise le login
	$connected=checklogin();
	
	// Charge la session
	$session=/dbObject/dbObject::executeSQL("select ")
	
	// Affiche le nom de l'équipe (holon)
	
	// Affiche le nom de la quête
	
	// Charge les quêtes actuelles
	
	// Charge les quêtes déjà faites
	
	// Charge les prochaines quêtes (celles dont les parents sont dans les quêtes actuelles)
	
	// Charge le niveau actuel (le max des niveaux dont toutes les quêtes sont faites)
	
	// Affiche l'interface

?>
